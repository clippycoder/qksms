"content":"Created At: 2026-07-12T03:50:48Z\nCompleted At: 2026-07-12T03:50:48Z\n\n\t\t\t\tThe command completed successfully.\n\t\t\t\tOutput:\n\t\t\t\t<truncated 56 lines>\n             messageBackground.setBackgroundTint(resolveThemeColor(R.attr.bubbleColor))\n         }\n     }\n \n+    override fun dispatchKeyEvent(event: KeyEvent): Boolean {\n+        if (event.keyCode in listOf(KeyEvent.KEYCODE_DPAD_LEFT, KeyEvent.KEYCODE_DPAD_RIGHT, KeyEvent.KEYCODE_DPAD_UP, KeyEvent.KEYCODE_DPAD_DOWN)) {\n+            if (event.action == KeyEvent.ACTION_DOWN) {\n+                isIntentionalNavigation = true\n+                message.post { isIntentionalNavigation = false }\n+            }\n+        }\n+        return super.dispatchKeyEvent(event)\n+    }\n+\n+    override fun dispatchTouchEvent(ev: MotionEvent): Boolean {\n+        if (ev.action == MotionEvent.ACTION_DOWN) {\n+            isIntentionalNavigation = true\n+            message.post { isIntentionalNavigation = false }\n+        }\n+        return super.dispatchTouchEvent(ev)\n+    }\n+\n     override fun onStart() {\n         super.onStart()\n         activityVisibleIntent.onNext(true)\n@@ -172,7 +213,14 @@ class ComposeActivity : QkThemedActivity(), ComposeView {\n         activityVisibleIntent.onNext(false)\n     }\n \n+    private var initialFocusDone = false\n+    private var lastThreadId = 0L\n+\n     override fun render(state: ComposeState) {\n+        lastState = state\n+        Timber.d(\"FOCUS-DIAG render(): editing=%b loading=%b attaching=%b threadId=%d initialFocusDone=%b msgHasFocus=%b msgVisible=%b\",\n+                state.editingMode, state.loading, state.attaching, state.threadId, initialFocusDone,\n+                message.hasFocus(), message.visibility == View.VISIBLE)\n         if (state.hasError) {\n             finish()\n             return\n@@ -192,7 +240,26 @@ class ComposeActivity : QkThemedActivity(), ComposeView {\n \n         toolbarTitle.setVisible(!state.editingMode)\n         chips.setVisible(state.editingMode)\n-        composeBar.setVisible(!state.loading)\n+        \n+        val wasLoading = loading.visibility == View.VISIBLE\n+        if (wasLoading != state.loading) {\n+            composeBar.setVisible(!state.loading)\n+            loading.setVisible(state.loading)\n+        }\n+\n+        // Auto-focus the message box when the conversation becomes ready.\n+        // For new conversations, threadId transitions from 0 → positive when resolved.\n+        // For existing conversations, threadId is positive from the first render.\n+        if (!initialFocusDone && state.threadId > 0 && !state.loading) {\n+            initialFocusDone = true\n+            message.post {\n+                if (!message.hasFocus()) {\n+                    message.requestFocus()\n+                    showKeyboard()\n+                }\n+            }\n+        }\n+        lastThreadId = state.threadId\n \n         // Don't set the adapters unless needed\n         if (state.editingMode && chips.adapter == null) chips.adapter = chipsAdapter\n@@ -212,8 +279,6 @@ class ComposeActivity : QkThemedActivity(), ComposeView {\n \n         chipsAdapter.data = state.selectedChips\n \n-        loading.setVisible(state.loading)\n-\n         sendAsGroup.setVisible(state.editingMode && state.selectedChips.size >= 2)\n         sendAsGroupSwitch.isChecked = state.sendAsGroup\n \n@@ -227,8 +292,33 @@ class ComposeActivity : QkThemedActivity(), ComposeView {\n         attachments.setVisible(state.attachments.isNotEmpty())\n         attachmentAdapter.data = state.attachments\n \n-        attach.animate().rotation(if (state.attaching) 135f else 0f).start()\n-        attaching.isVisible = state.attaching\n+        val wasAttaching = attaching.visibility == View.VISIBLE\n+        if (wasAttaching != state.attaching) {\n+            attach.animate().rotation(if (state.attaching) 135f else 0f).start()\n+            attaching.isVisible = state.attaching\n+        }\n+        \n+        if (state.attaching) {\n+            attach.nextFocusRightId = R.id.attach\n+            attach.nextFocusUpId = R.id.camera\n+            attach.nextFocusDownId = R.id.contact\n+            attach.nextFocusLeftId = R.id.attach\n+        } else {\n+            attach.nextFocusRightId = View.NO_ID\n+            attach.nextFocusUpId = View.NO_ID\n+            attach.nextFocusDownId = View.NO_ID\n+            attach.nextFocusLeftId = View.NO_ID\n+        }\n+        \n+        if (state.attaching && !wasAttaching) {\n+            attach.requestFocus()\n+        } else if (!state.attaching && wasAttaching) {\n+            // Attach menu just closed — restore focus to message box\n+            message.post {\n+                message.requestFocus()\n+                showKeyboard()\n+            }\n+        }\n \n         counter.text = state.remaining\n         counter.setVisible(counter.text.isNotBlank())\n@@ -239,6 +329,7 @@ class ComposeActivity : QkThemedActivity(), ComposeView {\n \n         send.isEnabled = state.canSend\n         send.imageAlpha = if (state.canSend) 255 else 128\n+        refreshSoftkeys()\n     }\n \n     override fun clearSelection() = messageAdapter.clearSelection()\n@@ -399,4 +490,82 @@ class ComposeActivity : QkThemedActivity(), ComposeView {\n \n     override fun onBackPressed() = backPressedIntent.onNext(Unit)\n \n+    override fun onCsk() {\n+        val state = lastState ?: return\n+        val focus = window.currentFocus\n+        Timber.d(\"FOCUS-DIAG onCsk(): focus=%s focusClickable=%b msgHasFocus=%b editing=%b loading=%b msgVisible=%b msgIsFocusable=%b\",\n+                focus?.javaClass?.simpleName ?: \"null\", focus?.isClickable ?: false,\n+                message.hasFocus(), state.editingMode, state.loading,\n+                message.visibility == View.VISIBLE, message.isFocusable)\n+        \n+        if (state.selectedMessages > 0) {\n+            Timber.d(\"FOCUS-DIAG onCsk: branch=deleteSelected\")\n+            toolbar?.menu?.findItem(R.id.delete)?.let { onOptionsItemSelected(it) }\n+        } else if (focus != null && focus != messageList && focus.isClickable && focus != message) {\n+            Timber.d(\"FOCUS-DIAG onCsk: branch=clickFocused (%s)\", focus.javaClass.simpleName)\n+            focus.performClick()\n+        } else if (message.hasFocus()) {\n+            Timber.d(\"FOCUS-DIAG onCsk: branch=send\")\n+            send.performClick()\n+        } else {\n+            Timber.d(\"FOCUS-DIAG onCsk: branch=focusMessage\")\n+            val result = message.requestFocus()\n+            Timber.d(\"FOCUS-DIAG onCsk: requestFocus returned %b\", result)\n+            showKeyboard()\n+        }\n+    }\n+\n+    override fun onSk1() {\n+        val state = lastState ?: return\n+        if (state.selectedMessages > 0) {\n+            clearSelection()\n+        } else if (message.hasFocus()) {\n+            // IME mode - SK1 is empty\n+        } else {\n+            attach.performClick()\n+        }\n+    }\n+\n+    override fun onSk2() {\n+        val state = lastState ?: return\n+        if (message.hasFocus()) {\n+            // Kyocera IME controls SK2 while typing — no-op\n+        } else {\n+            // No action when not typing\n+        }\n+    }\n+\n+    override fun refreshSoftkeys() {\n+        if (!kyoceraHelper.isReady()) return\n+\n+        val state = lastState ?: return\n+\n+        if (state.selectedMessages > 0) {\n+            kyoceraHelper.apply(\n+                com.moez.QKSMS.common.util.KyoceraSoftkeyHelper.PRIORITY_LIST,\n+                getString(R.string.sk_delete),\n+                getString(R.string.sk_deselect),\n+                getString(R.string.sk_options)\n+            )\n+        } else if (message.hasFocus()) {\n+            // Message box has focus — set CSK to \"OK\" so the onCsk() callback\n+            // stays enabled. Setting it to empty disables the callback entirely.\n+            // The Kyocera IME will override the label display at PRIORITY_TEXT,\n+            // but our callback still fires.\n+            kyoceraHelper.apply(\n+                com.moez.QKSMS.common.util.KyoceraSoftkeyHelper.PRIORITY_LIST,\n+                getString(R.string.sk_ok),\n+                \"\",\n+                \"\"\n+            )\n+        } else {\n+            kyoceraHelper.apply(\n+                com.moez.QKSMS.common.util.KyoceraSoftkeyHelper.PRIORITY_LIST,\n+                getString(R.string.sk_ok),\n+                \"\",\n+                \"\"\n+            )\n+        }\n+    }\n+\n }\n\n"
